[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "TrenchChat"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "rns",
    "lxmf",
    "PyQt6",
    "msgpack",
]

[project.gui-scripts]
trenchchat = "main:main"
