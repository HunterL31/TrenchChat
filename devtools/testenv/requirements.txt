# Dependencies for the local two-tester test environment (devtools/testenv/).
# Dev-only -- install into the same .venv as the main requirements.txt before
# running orchestrator.py or smoke_test.py. The server stack the app itself
# needs (fastapi, uvicorn, websockets) lives in requirements.txt: main_flutter.py
# serves api.py, so it is not a dev extra.

httpx>=0.28
