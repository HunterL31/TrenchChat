# Generated by prepare.sh / prepare.ps1 — a vendored copy of trenchchat/core,
# not a second source of truth. Re-run prepare after checkout.
python/app/trenchchat/

# Flutter/Dart build output
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
